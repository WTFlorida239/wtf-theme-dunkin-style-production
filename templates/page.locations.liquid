{% comment %}
  Template: page.locations.liquid
  Store locations page with hours, map, and local SEO
{% endcomment %}

<div class="page-locations">
  <div class="page-width">
    <header class="section-header">
      <h1 class="section-header__title">{{ page.title }}</h1>
    </header>
    
    {% section 'locations' %}
  </div>
</div>